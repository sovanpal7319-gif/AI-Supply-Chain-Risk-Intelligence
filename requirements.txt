# ── Core Dependencies (always required) ──────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.8.0
pydantic-settings==2.4.0
python-dotenv==1.0.1
loguru==0.7.2
httpx==0.27.0
numpy>=1.24.0

# ── LLM Provider ─────────────────────────────────────────
openai==1.40.0

# ── Neo4j ────────────────────────────────────────────────
neo4j==5.23.0

# ── BERT Model Dependencies ──────────────────────────────
torch>=2.0.0
transformers>=4.30.0

# ── GraphSAGE Dependencies ───────────────────────────────
# Install with pre-built wheels to avoid slow C++ compilation:
#   pip install torch-geometric torch-scatter torch-sparse \
#       -f https://data.pyg.org/whl/torch-2.4.0+cpu.html
# Replace 'torch-2.4.0+cpu' with your PyTorch version.
torch-geometric>=2.4.0
torch-scatter>=2.1.0
torch-sparse>=0.6.0

# ── SPERT NER+RE Dependencies ────────────────────────────
spacy>=3.0.0
tqdm>=4.50.0
